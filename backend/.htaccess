Options -Indexes

RewriteEngine On

# Pass real files/dirs through (e.g. api-docs.html, vendor assets)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Everything else → front controller
RewriteRule ^ index.php [QSA,L]
