# backend/.env.example
# Copy to .env and fill in real values

# ── Database ─────────────────────────────────────
DB_HOST=localhost
DB_NAME=tdd_management
DB_USER=root
DB_PASS=

# ── Database Connection Pool ──────────────────────
# DB_PERSISTENT=true reuses the same MySQL socket across PHP-FPM requests
# (acts as a lightweight connection pool — no separate pooler needed for PHP-FPM).
# Set to false only if you use a dedicated proxy like ProxySQL or PgBouncer.
DB_PERSISTENT=true
DB_CONNECT_TIMEOUT=10

# ── App ──────────────────────────────────────────
APP_ENV=development        # development | production
APP_URL=http://localhost:8000
JWT_SECRET=CHANGE_THIS_TO_A_LONG_RANDOM_SECRET
ALLOWED_ORIGINS=http://localhost:4200

# ── WhatsApp ─────────────────────────────────────
# Provider: meta | twilio | log (dev/testing)
WHATSAPP_PROVIDER=meta

# Meta Cloud API (WhatsApp Business)
META_WHATSAPP_TOKEN=
META_PHONE_NUMBER_ID=

# Twilio WhatsApp Sandbox
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=+14155238886

# ── QR Code ──────────────────────────────────────
# Leave empty to use Google Charts API fallback
# Install endroid/qr-code via composer for local gen
